import numpy as np
from numpy import linalg as LA
import matplotlib.pyplot as plt


tmp = np.array([(1+1j), (2+3j)])
tmp2 = tmp.real
print(tmp2)
# lam1 = complex(1, 2)
# lam2 = complex(1, -2)
# real_deal = [lam1.real, lam2.real]
# not_real_deal = [lam1.imag, lam2.imag]

# plt.scatter(real_deal, not_real_deal)
# plt.show()